var minhaVar = 'minha variável';
function minhaFuncao(x, y) {
    return x + y;
}
// ES6 ou ES2015
var numero = 2;
var numeroPi = 3.14;
var numeros = [1, 2, 3];
numeros.map(function (valor) {
    return valor * 2;
});
numeros.map(function (valor) {
    return valor * 2;
});
numeros.map(function (valor) { return valor * 2; }); //ES2015
var MatematicaCalculo = /** @class */ (function () {
    function MatematicaCalculo() {
    }
    MatematicaCalculo.prototype.soma = function (x, y) {
        return x + y;
    };
    return MatematicaCalculo;
}());
var n1 = 'sdisid';
n1 = 3;
